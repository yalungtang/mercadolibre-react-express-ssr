{
  "name": "marketplace-search-api",
  "version": "1.0.0",
  "description": "A simplified version of a search engine",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./src --out-dir dist/ --copy-files",
    "start:prod": "npm run build && babel-node dist/index.js",
    "start:dev": "nodemon --exec babel-node ./src"
  },
  "author": "Yalung Tang",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "core-util-is": "^1.0.2",
    "express": "^4.17.1",
    "nodemon": "^2.0.4"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.3",
    "@babel/core": "^7.10.3",
    "@babel/node": "^7.10.3",
    "@babel/preset-env": "^7.10.3",
    "rimraf": "^3.0.2"
  }
}
